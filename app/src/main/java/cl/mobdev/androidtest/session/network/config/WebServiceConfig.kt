package cl.mobdev.androidtest.session.network.config

object WebServiceConfig {
    object Url {
        const val URL_PROD = "https://padok-bff.herokuapp.com"
    }

    object Timeout {
        const val CONNECT: Long = 30
    }
}
